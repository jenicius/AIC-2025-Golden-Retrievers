// Query by text
export const queryByText = (text: string, topK: number, model: string, metric: string) => {
    console.log("Query By Text")
};

// Query by image
export const queryByImage = (image: File, topK: number, model: string, metric: string) => {
    console.log("Query By Image")
};

// Query by OCR text
export const queryByOCR = (text: string, topK: number, model: string, metric: string) => {
    console.log("Query By OCR")
};
